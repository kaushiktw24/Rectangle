public class Rectangle {

    public int calculateRectangleArea(int width, int length)
    {
        return width * length;
    }

    public int calculateRectanglePerimeter(int width, int length)
    {
        return (length + width) * 2;
    }
}
